import React, { Fragment } from "react";

const BasicFunction = (props) => {
  return (
    <Fragment>
        <h2>Welcome {props.name} to BasicFunction</h2>
        
    </Fragment>
  );
}
export default BasicFunction